# Local application data

- `app/` contains the ignored SQLite database.
- `manifests/` will contain small version-controlled metadata files.
- `demo/media/` contains ignored prepared presentation media.

Full datasets never belong in this directory or in Git.
