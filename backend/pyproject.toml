[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "smart-parking-backend"
version = "1.0.0"
description = "Artificial Intelligence Based Smart Parking System backend"
requires-python = ">=3.12,<3.14"
dependencies = [
  "fastapi==0.141.1",
  "imagehash==4.3.2",
  "numpy==2.5.2",
  "onnx==1.20.1",
  "onnxruntime==1.29.0",
  "opencv-python-headless==4.13.0.92",
  "pillow==12.3.0",
  "psutil==7.2.2",
  "pydantic-settings==2.15.0",
  "python-multipart==0.0.22",
  "scikit-learn==1.9.0",
  "sqlalchemy==2.0.52",
  "torch==2.14.0",
  "torchvision==0.29.0",
  "uvicorn[standard]==0.52.4",
]

[project.optional-dependencies]
dev = [
  "httpx==0.28.1",
  "pytest==9.1.1",
  "pytest-cov==7.1.0",
  "ruff==0.16.5",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
